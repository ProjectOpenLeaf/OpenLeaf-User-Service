apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-profile-service
  namespace: openleaf
  labels:
    app: user-profile-service
    tier: backend
    version: v1
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0

  selector:
    matchLabels:
      app: user-profile-service

  template:
    metadata:
      labels:
        app: user-profile-service
        tier: backend
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8083"
        prometheus.io/path: "/actuator/prometheus"

    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - user-profile-service
                topologyKey: kubernetes.io/hostname

      containers:
        - name: user-profile-service
          image: ghcr.io/projectopenleaf/openleaf-user-profile-service:latest
          imagePullPolicy: Always

          ports:
            - name: http
              containerPort: 8083
              protocol: TCP

          envFrom:
            - configMapRef:
                name: user-service-config
            - secretRef:
                name: user-service-secret

          resources:
            requests:
              memory: "256Mi"    # Reduced from 512Mi
              cpu: "200m"        # Reduced from 500m
            limits:
              memory: "512Mi"    # Reduced from 1Gi
              cpu: "500m"

      #          livenessProbe:
#            httpGet:
#              path: /actuator/health
#              port: 8083
#            initialDelaySeconds: 60
#            periodSeconds: 30
#            timeoutSeconds: 10
#            failureThreshold: 3
#
#          readinessProbe:
#            httpGet:
#              path: /actuator/health
#              port: 8083
#            initialDelaySeconds: 30
#            periodSeconds: 10
#            timeoutSeconds: 5
#            failureThreshold: 3

      imagePullSecrets:
        - name: ghcr-secret

---
apiVersion: v1
kind: Service
metadata:
  name: user-profile-service
  namespace: openleaf
  labels:
    app: user-profile-service
spec:
  type: ClusterIP
  selector:
    app: user-profile-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-profile-service-hpa
  namespace: openleaf
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user-profile-service

  minReplicas: 1      # Change from 2 - reduce startup load
  maxReplicas: 10

  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70

    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80

  behavior:
    scaleUp:
      stabilizationWindowSeconds: 300  # Wait 5 mins before scaling
      policies:
        - type: Pods
          value: 1           # Only add 1 pod at a time
          periodSeconds: 60
        - type: Percent
          value: 50
          periodSeconds: 60
      selectPolicy: Max

    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 1
          periodSeconds: 60
        - type: Percent
          value: 10
          periodSeconds: 60
      selectPolicy: Min